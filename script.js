// Function to toggle the visibility of the menu
function toggleMenu() {
  const menu = document.getElementById("menu"); // Gets the menu by its ID
  menu.classList.toggle("hidden"); // Adds or removes the 'hidden' class
  menu.classList.toggle("visible"); // Adds or removes the 'visible' class
}
